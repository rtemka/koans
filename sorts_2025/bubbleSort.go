package sorts2025

func BubbleSort(arr []int) []int {
	for i := range len(arr) {
		for j := i + 1; j < len(arr); j++ {
			if arr[i] > arr[j] {
				arr[i], arr[j] = arr[j], arr[i]
			}
		}
	}
	return arr
}
